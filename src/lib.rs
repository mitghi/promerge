extern crate pest_derive;

use pest::Parser;
use pest_derive::Parser as Parse;

pub mod promerge;
mod parser;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
